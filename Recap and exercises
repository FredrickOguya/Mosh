# Create a map() that returns the length of each name.

students = ['Fred', 'Liam', 'Vivian', 'Brian', 'Alice']

name_length = map(lambda student:len(student),students)

print(list(name_length))

#create another map() that turns all names into lowercase

lowercased = map(lambda student: student.lower(),students)

print(list(lowercased))

