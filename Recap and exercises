# ✅ Challenge 1: Add & Remove
# Start with this list:


fruits = ["apple", "banana"]
#Tasks:

# Add "orange" at the end.

fruits.append("orange")

# Insert "mango" at index 1.

fruits.insert(1,"mango")

# Remove "banana".

fruits.remove("banana")

# Print the final list.

print(fruits)

# ✅ Challenge 2: Find & Count
# Use this list:


scores = [88, 92, 75, 88, 100]
# Tasks:

# Check if 88 is in the list.

print(88 in scores)

# Find the index of 75.

print(scores.index(75))

# Count how many times 88 appears.

print(scores.count(88))

# ✅ Challenge 3: Sorting

nums = [5, 2, 9, 1, 7]
# Tasks:

# Sort the list in ascending order.

nums.sort()

# Then sort it in descending order.
nums.sort(reverse=True)
# ✅ Challenge 4: Lambda + Map

numbers = [1, 2, 3, 4, 5]
# Task:
# Use map() and a lambda function to create a new list where each number is doubled.

doubled_numbers = list(map(lambda x:x*2,numbers))

# ✅ Challenge 5: Zip Combo

names = ["Alice", "Bob", "Eve"]
roles = ["Leader", "Tester", "Developer"]
# Task:
# Use zip() and list comprehension to format:

output = list(f"{name} is the {role}" for name,role in zip(names,roles))

print(output)
# ["Alice is the Leader", "Bob is the Tester", "Eve is the Developer"]