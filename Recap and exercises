# 🧠 Challenge 1: Add "Mr./Ms." Titles
# Given this list:

names = ['John', 'Mary', 'Alex', 'Grace']
# Create a map() function that returns:

['Mr. John', 'Ms. Mary', 'Mr. Alex', 'Ms. Grace']
# 💡 Rule:

# Use "Mr." if the name starts with a consonant

# Use "Ms." if the name starts with a vowel (a, e, i, o, u)


titled = map(
    lambda name: "Ms. " + name if name[0].lower() in 'aeiou' else "mr. " + name,
    names
)

print(list(titled))